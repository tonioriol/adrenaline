we should have a simple setting saying: prevent sleep even with lid closed, and if we want to keep the screen on (and disk on?) id say should be default to stop scren when lid closed and disk... well whats the point of keeping disks off if you want the computer on?

lock screen when closing lid? we certainly want the screen stopped, but i guess thats not somethin we can even do. but im not sure if we should lock the screen, if you lock it the stuff running will be still running anyway?

--

use insomnia molecular structure as icon of the app

--

repair/install helper should go from menu. if the app is installed all should be good to go, or do we really need that? for which cases?

--

* menu goes away when changing a checkbox and shouldnt
* play lid sounds should only apply when prevent lid is enabled
* lock screen shouldnt be dependent on lid setting: should be a standalone optoion
* what abput the disk sleep
* launch at login option checkbox

--

usually on first run the sounds dont play probably because not cached?
