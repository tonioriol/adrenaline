name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pull-requests: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  decide:
    runs-on: ubuntu-latest
    outputs:
      will_release: ${{ steps.dryrun.outputs.new_release_published }}
      next_version: ${{ steps.dryrun.outputs.new_release_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Install dependencies
        run: npm ci

      - name: Semantic Release dry run
        id: dryrun
        uses: cycjimmy/semantic-release-action@v4
        with:
          dry_run: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  release:
    needs: decide
    if: needs.decide.outputs.will_release == 'true'
    runs-on: macos-14
    env:
      NEXT_VERSION: ${{ needs.decide.outputs.next_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Install dependencies
        run: npm ci

      - name: Import Developer ID certificate
        env:
          CERTIFICATE_BASE64: ${{ secrets.APPLE_DEVELOPER_ID_CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.APPLE_DEVELOPER_ID_CERTIFICATE_PASSWORD }}
          CERTIFICATE_PATH: ${{ runner.temp }}/certificate.p12
          KEYCHAIN_PATH: ${{ runner.temp }}/build.keychain
          KEYCHAIN_PASSWORD_FILE: ${{ runner.temp }}/keychain-password
        run: |
          set -euo pipefail
          echo "$(uuidgen)" > "$KEYCHAIN_PASSWORD_FILE"
          security create-keychain -p "$(cat "$KEYCHAIN_PASSWORD_FILE")" "$KEYCHAIN_PATH"
          security default-keychain -s "$KEYCHAIN_PATH"
          security unlock-keychain -p "$(cat "$KEYCHAIN_PASSWORD_FILE")" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          echo "$CERTIFICATE_BASE64" | base64 --decode > "$CERTIFICATE_PATH"
          security import "$CERTIFICATE_PATH" -k "$KEYCHAIN_PATH" -P "$CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$(cat "$KEYCHAIN_PASSWORD_FILE")" "$KEYCHAIN_PATH"
          CODE_SIGN_IDENTITY="$(security find-identity -v -p codesigning "$KEYCHAIN_PATH" | awk -F'"' '/Developer ID Application/ {print $2; exit}')"
          if [ -z "$CODE_SIGN_IDENTITY" ]; then
            echo "No Developer ID Application signing identity found in temporary keychain" >&2
            exit 1
          fi
          echo "CODE_SIGN_IDENTITY=$CODE_SIGN_IDENTITY" >> "$GITHUB_ENV"

      - name: Configure notarytool profile
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_NOTARYTOOL_PROFILE: ${{ secrets.APPLE_NOTARYTOOL_PROFILE }}
        run: |
          set -euo pipefail
          xcrun notarytool store-credentials "$APPLE_NOTARYTOOL_PROFILE" \
            --apple-id "$APPLE_ID" \
            --team-id "$APPLE_TEAM_ID" \
            --password "$APPLE_APP_SPECIFIC_PASSWORD"

      - name: Bump Info.plist versions
        run: |
          set -euo pipefail
          /usr/libexec/PlistBuddy \
            -c "Set :CFBundleShortVersionString ${NEXT_VERSION}" \
            -c "Set :CFBundleVersion ${NEXT_VERSION}" \
            Resources/Insomnia/Info.plist
          /usr/libexec/PlistBuddy \
            -c "Set :CFBundleVersion ${NEXT_VERSION}" \
            Resources/InsomniaHelper/Info.plist
          echo "Bumped to ${NEXT_VERSION}:"
          /usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" Resources/Insomnia/Info.plist
          /usr/libexec/PlistBuddy -c "Print :CFBundleVersion" Resources/Insomnia/Info.plist
          /usr/libexec/PlistBuddy -c "Print :CFBundleVersion" Resources/InsomniaHelper/Info.plist

      - name: Build signed app
        run: |
          set -euo pipefail
          make release-zip CONFIGURATION=release CODE_SIGN_IDENTITY="$CODE_SIGN_IDENTITY"
          codesign --verify --deep --strict --verbose=2 build/Insomnia.app

      - name: Notarize app zip
        env:
          APPLE_NOTARYTOOL_PROFILE: ${{ secrets.APPLE_NOTARYTOOL_PROFILE }}
        run: |
          set -euo pipefail
          xcrun notarytool submit build/Insomnia.zip --keychain-profile "$APPLE_NOTARYTOOL_PROFILE" --wait
          xcrun stapler staple build/Insomnia.app
          xcrun stapler validate build/Insomnia.app
          spctl --assess --type execute --verbose=4 build/Insomnia.app
          rm -f build/Insomnia.zip
          ditto -c -k --keepParent build/Insomnia.app "build/Insomnia-v${NEXT_VERSION}.zip"

      - name: Sign update with EdDSA
        shell: bash
        env:
          SPARKLE_ED_PRIVATE_KEY: ${{ secrets.SPARKLE_ED_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          SPARKLE_VERSION=$(python3 - <<'PY'
          import json
          with open('Package.resolved') as f:
              data = json.load(f)
          for pin in data['pins']:
              if pin['identity'] == 'sparkle':
                  print(pin['state']['version'])
                  break
          else:
              raise SystemExit('Sparkle pin not found in Package.resolved')
          PY
          )
          curl -fsSL "https://github.com/sparkle-project/Sparkle/releases/download/${SPARKLE_VERSION}/Sparkle-${SPARKLE_VERSION}.tar.xz" \
            | tar -xJf - -C "$RUNNER_TEMP" "./bin/sign_update"
          SIGN_UPDATE="$RUNNER_TEMP/bin/sign_update"
          SIG_AND_LEN=$("$SIGN_UPDATE" --ed-key-file <(printf "%s" "$SPARKLE_ED_PRIVATE_KEY") "build/Insomnia-v${NEXT_VERSION}.zip")
          echo "SPARKLE_SIG_AND_LEN=$SIG_AND_LEN" >> "$GITHUB_ENV"

      - name: Semantic Release (commit, tag, publish, upload asset)
        uses: cycjimmy/semantic-release-action@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish appcast entry
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ env.NEXT_VERSION }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          DOWNLOAD_URL="https://github.com/${GH_REPO}/releases/download/v${VERSION}/Insomnia-v${VERSION}.zip"
          PUB_DATE=$(date -u +"%a, %d %b %Y %H:%M:%S +0000")
          RELEASE_NOTES=$(gh release view "v${VERSION}" --json body -q .body | head -c 8000)

          git clone --depth 1 --branch gh-pages \
            "https://x-access-token:${GITHUB_TOKEN}@github.com/${GH_REPO}.git" gh-pages
          cd gh-pages

          if grep -q "<title>Insomnia ${VERSION}</title>" appcast.xml; then
            echo "Appcast already contains v${VERSION}, skipping"
            exit 0
          fi

          # Inject new <item> before </channel> using python (BSD awk
          # rejects multi-line -v values on macOS runners).
          export VERSION DOWNLOAD_URL PUB_DATE RELEASE_NOTES SPARKLE_SIG_AND_LEN
          python3 - <<'PY'
          import os, sys
          version = os.environ['VERSION']
          item = f"""        <item>
              <title>Insomnia {version}</title>
              <pubDate>{os.environ['PUB_DATE']}</pubDate>
              <sparkle:version>{version}</sparkle:version>
              <sparkle:shortVersionString>{version}</sparkle:shortVersionString>
              <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
              <description><![CDATA[{os.environ['RELEASE_NOTES']}]]></description>
              <enclosure url="{os.environ['DOWNLOAD_URL']}"
                         type="application/octet-stream"
                         {os.environ['SPARKLE_SIG_AND_LEN']} />
          </item>"""
          with open('appcast.xml', 'r') as f:
              content = f.read()
          if '</channel>' not in content:
              sys.exit('appcast.xml has no </channel> close tag')
          content = content.replace('</channel>', item + '\n    </channel>', 1)
          with open('appcast.xml', 'w') as f:
              f.write(content)
          PY

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add appcast.xml
          git commit -m "appcast: v${VERSION}"
          git push origin gh-pages

      - name: Cleanup signing materials
        if: always()
        env:
          CERTIFICATE_PATH: ${{ runner.temp }}/certificate.p12
          KEYCHAIN_PATH: ${{ runner.temp }}/build.keychain
        run: |
          set -euo pipefail
          rm -f "$CERTIFICATE_PATH"
          security default-keychain -s login.keychain-db || true
          security delete-keychain "$KEYCHAIN_PATH" || true
